services:
  caddy:
    image: caddy:alpine
    restart: always
    container_name: caddy
    volumes:
      - $(pwd)/Caddyfile:/etc/caddy/Caddyfile
      - ./certs:/certs
      - ./config:/config
      - ./data:/data
      - $(pwd)/sites:/srv
    network_mode: "host"
